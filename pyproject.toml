[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-eval-ragas"
version = "0.1.0"
description = "RAG pipeline evaluation with RAGAS"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
